# Known Limitations

- The RAG implementation uses a lightweight TF-IDF index rather than a vector database.
- The sample alarm corpus is intentionally small and synthetic.
- The UI shows a simplified end-to-end workflow suitable for assignment delivery rather than a full enterprise-grade deployment.
